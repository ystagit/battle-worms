class TextureInfo {

    constructor(name, width, height, textureId) {
        this.name = name
        this.width = width
        this.height = height
        this.textureId = textureId
    }
}

export default TextureInfo